apiVersion: v1
kind: pod
metadata:
  name: simple-webapp-color-blue 
  labels:
    app: web
spec:
  containers:
  - image: mmumshad/simple-webapp-color
    name: web
    ports:
      - containerPort: 8080
    env:
      - name: APP_COLOR
        value: blue
        